namespace Bank
{
    public class Class1
    {
        //make a bank class
        //bank has an array of accounts (account is another class)
        //account has general info (money, username, password)

        //make menu to login and deposit/withdraw/transfer money

        //Bonus: make the account array work the same was as shopping list (expand/contract)
    }
}